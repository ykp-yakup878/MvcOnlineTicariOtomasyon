@model IEnumerable<MvcOnlineTicariOtomasyon.Models.siniflar.SinifGrup3>
<div class="card">
    <div class="card-header">
        <h3 class="card-title">Marka Tablosu</h3>
    </div>
    <!-- /.card-header -->
    <div class="card-body p-0">
        <table class="table table-striped">
            <thead>
                <tr>
                    <th style="width: 10px">#</th>
                    <th>Marka</th>
                    <th>Progress Bar</th>
                    <th style="width: 40px">Değer</th>
                </tr>
            </thead>
            <tbody>
                @{ int no = 0;}
                @foreach (var x in Model)
                {
                    <tr>
                        <td>@{no++;}</td>
                        <td>@x.Marka</td>
                        <td>
                            @if (x.Sayi >= 1 && x.Sayi <= 3)
                            {
                                <div class="progress progress-xs">
                                    <div class="progress-bar bg-warning" style="width: 30%"></div>
                                </div>
                            }
                            @if (x.Sayi >= 4 && x.Sayi <= 6)
                            {
                                <div class="progress progress-xs">
                                    <div class="progress-bar bg-success" style="width: 70%"></div>
                                </div>
                            }
                        </td>
                        <td>
                            @if (x.Sayi >= 1 && x.Sayi <= 3)
                            {
                                <span class="badge bg-danger">@x.Sayi</span>
                            }
                            @if (x.Sayi >= 4 && x.Sayi <= 6)
                            {
                                <span class="badge bg-success">@x.Sayi</span>
                            }
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
